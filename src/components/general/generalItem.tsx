import React from 'react';

// export const GeneralItem = (name: any, fullname: any, image: any) => {
export const GeneralItem = () => {
    return (
        <div className="card">
            {/*<img src={image} alt={'heroe'} />
            <p>{name}</p>
            <p>{fullname}</p>*/}
        </div>
    )
}
